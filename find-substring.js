/*
  To check substring is present in a string or not
  -------------
  @params {string, string}
  @return boolean
*/

var isSubStringMatch = function(word, subString) {
  return word.indexOf(subString) > -1;
};
