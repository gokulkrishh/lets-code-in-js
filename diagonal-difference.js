/*
  Calculate absolute difference b/w sum of its diagonals
  -------------
  @params {Array}
  @return number
*/

var diagonalDifference = function(arr, noOfRows) {
  var temp = 0;

  for (var i = 0; i < arr.length; i++) {
    var row = arr.slice(0, noOfRows);
    for (var j = 0; j < row.length; j++) {

    }
  }
};
